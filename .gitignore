# macOS / OS metadata
.DS_Store
**/.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Backups y temporales
*.bak
*.tmp
*.orig
~$*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Secretos / config local — NUNCA commitear
.env
.env.*
!.env.example
*.pem
*.key
.dev.vars

# Cloudflare Workers / Pages
.wrangler/
.mf/
wrangler.toml.local

# Build artifacts
dist/
build/
.next/
.cache/

# Artefactos del montaje FUSE (Cowork) — nunca commitear
.fuse_hidden*

# LibreOffice lock files
.~lock.*#

# Tutoría — datos personales de menores.
# Nunca deben entrar en el repositorio: viven en Cloudflare D1 y R2, y los
# scripts que los manejan escriben fuera del repo. Esto es solo una red de
# seguridad.
Orla*.pdf
orla*.pdf
orla-*.html
fitxes-*.json
tutoria-*/
# …pero /docencia/tutoria-*/ es la parte pública de la tutoría (horario
# del grupo, presentación): HTML generado, sin ningún dato de alumnos.
!docencia/tutoria-*/
!ca/docencia/tutoria-*/
!en/docencia/tutoria-*/
alumnes.sql
**/fotos-alumnes/
