#!/usr/bin/env python3
"""[DEPRECATED] One-shot scaffolder used to build the T2P1 exam pages on
26 abr 2026. The HTML is now committed under
`aula/ccss-1btl/examenes/2526-1btl-macs-t2p1/p*.html` and edited there directly.

This file is intentionally left as a no-op so the canonical workflow stays
`build_ejercicios.py` + `build_exam_pages.py` + `build_active_subjects.py`.
"""

if __name__ == "__main__":
    print("This scaffolder has already run for T2P1; nothing to do.")
